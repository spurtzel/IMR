"""Backend implementations: the Trino backend subpackage.

,,backends/trino/'' is the shipped backend; the interface (,,SqlBackend'', the
,,backend_for'' registry) lives in ,,eimer.sql.backend''.
"""
